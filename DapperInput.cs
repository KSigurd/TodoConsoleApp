using System.Data.SqlClient;
using System.Collections.Generic;
using Dapper;
namespace TodoApp
{
    


class DBRepository
{

/*


    public void UpdatePlayer(TodoItem todo)
    {
        using (SqlConnection connection = new SqlConnection(connectionString))
        {
            connection.Execute("UPDATE F8 SET FirstName = @FirstName WHERE Id = @Id", todo);
        }
    }

    public void RemovePlayer(int todoIndex)
    {
        using (SqlConnection connection = new SqlConnection(connectionString))
        {
            connection.Execute("DELETE FROM F8 WHERE Id = @Id", new { Id = todoIndex });
        }
    }
*/
}
}
